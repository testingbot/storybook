import { Meta } from '@storybook/addon-docs/blocks'

<Meta title="Basics/Palette" />

# Palette

A hand written MDX page, which is what `captureDocs` runs. It is not attached to
a component, so nothing else in this Storybook covers it: if the docs template
or these swatches regress, only a docs capture notices.

<div style={{ display: 'flex', gap: 8, marginTop: 16 }}>
  <div style={{ width: 96, height: 64, background: '#1a73e8' }} />
  <div style={{ width: 96, height: 64, background: '#188038' }} />
  <div style={{ width: 96, height: 64, background: '#b00020' }} />
  <div style={{ width: 96, height: 64, background: '#f9ab00' }} />
</div>
