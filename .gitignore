node_modules/
dist/
*.tgz
*.log
.DS_Store

# Tunnel artefacts
testingbot-tunnel*.jar
testingbot-tunnel*.pid

# Per-run screenshot output. Baselines under .testingbot/baselines are committed.
.testingbot/results/

# Serena tool state, local to a checkout
.serena/

# Built Storybook output, in the examples and anywhere else
storybook-static/

# Credentials. The addon writes TB_KEY and TB_SECRET here when the panel is
# given consent to persist them, and the README tells people to create it by
# hand, so it must never be committable.
.env
.env.*
!.env.example
